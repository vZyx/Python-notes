

str = 'mostafa'

# Compute length of the string
str_len = len(str)

print(str_len)      # 7
print(len(str))     # 7

# Don't use variable name same as function name
len = len(str)      # Don't
#len = len(str)     # Now error!


